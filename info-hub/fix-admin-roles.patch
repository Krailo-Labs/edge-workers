--- app/admin/page.tsx
+++ app/admin/page.tsx
@@ -179,37 +179,38 @@
               </p>
             </div>
 
-            <div className="overflow-x-auto">
-              <table className="w-full text-left text-sm">
-                <thead>
-                  <tr className="border-b border-stone-200 text-stone-400 text-xs uppercase tracking-wider">
-                    <th className="py-3 px-4 font-bold">Розділ / Маршрут</th>
-                    {ROLES.map(r => (
-                      <th key={r.role} className="py-3 px-4 font-bold text-center">{r.label}</th>
-                    ))}
-                  </tr>
-                </thead>
-                <tbody className="divide-y divide-stone-100">
-                  {NAV_ROUTES.map(route => (
-                    <tr key={route.path} className="hover:bg-stone-50/60 transition-colors">
-                      <td className="py-3 px-4 font-medium text-stone-800">
-                        {route.label}
-                        <div className="text-[11px] text-stone-400 font-mono">{route.path}</div>
-                      </td>
-                      {ROLES.map(r => {
-                        const currentMode = permissionsConfig.roles[r.role]?.navAccess[route.path] || 'VISIBLE';
-                        return (
-                          <td key={r.role} className="py-3 px-4">
-                            <select 
-                              value={currentMode}
-                              onChange={(e) => handleModeChange(r.role, route.path, e.target.value as VisibilityMode)}
-                              className={cn(
-                                "w-full min-w-[110px] text-xs font-medium rounded-xl px-2 py-1.5 border appearance-none text-center cursor-pointer outline-none focus:ring-2 focus:ring-emerald-200",
-                                currentMode === 'VISIBLE' ? 'bg-emerald-50 text-emerald-700 border-emerald-200' :
-                                currentMode === 'BLURRED_LOCKED' ? 'bg-amber-50 text-amber-700 border-amber-200' :
-                                'bg-stone-100 text-stone-500 border-stone-200'
-                              )}
-                            >
-                              <option value="VISIBLE">👀 Доступно</option>
-                              <option value="BLURRED_LOCKED">🔒 Замок</option>
-                              <option value="HIDDEN">❌ Сховано</option>
-                            </select>
-                          </td>
-                        );
-                      })}
-                    </tr>
-                  ))}
-                </tbody>
-              </table>
+            <div className="space-y-4">
+              {NAV_ROUTES.map(route => (
+                <div key={route.path} className="bg-stone-50 rounded-2xl p-4 border border-stone-100">
+                  <div className="mb-4 pb-3 border-b border-stone-200 flex flex-col sm:flex-row sm:items-center justify-between gap-2">
+                     <div>
+                        <h3 className="font-bold text-stone-900">{route.label}</h3>
+                        <div className="text-[11px] text-stone-400 font-mono">{route.path}</div>
+                     </div>
+                  </div>
+                  <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
+                    {ROLES.map(r => {
+                      const currentMode = permissionsConfig.roles[r.role]?.navAccess[route.path] || 'VISIBLE';
+                      return (
+                        <div key={r.role} className="flex flex-col gap-1.5">
+                          <span className="text-[10px] font-semibold text-stone-500 uppercase tracking-wider">{r.label}</span>
+                          <select 
+                            value={currentMode}
+                            onChange={(e) => handleModeChange(r.role, route.path, e.target.value as VisibilityMode)}
+                            className={cn(
+                              "w-full text-xs font-medium rounded-xl px-2 py-2 border appearance-none text-center cursor-pointer outline-none focus:ring-2 focus:ring-emerald-200 transition-colors",
+                              currentMode === 'VISIBLE' ? 'bg-emerald-50 text-emerald-700 border-emerald-200 hover:bg-emerald-100' :
+                              currentMode === 'BLURRED_LOCKED' ? 'bg-amber-50 text-amber-700 border-amber-200 hover:bg-amber-100' :
+                              'bg-stone-100 text-stone-500 border-stone-200 hover:bg-stone-200'
+                            )}
+                          >
+                            <option value="VISIBLE">👀 Доступно</option>
+                            <option value="BLURRED_LOCKED">🔒 Замок</option>
+                            <option value="HIDDEN">❌ Сховано</option>
+                          </select>
+                        </div>
+                      );
+                    })}
+                  </div>
+                </div>
+              ))}
             </div>
